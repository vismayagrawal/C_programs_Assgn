#include <stdio.h>
#include <conio.h>
void main()
{
    clrscr();
    float n,p,j,x;
    char c,y;
    do
    {
     printf("\n\nenter any no and its power\n");
     scanf(" %f%f",&x,&n);
     for(;n>=1;n--)
	{
	 p=1;
	 for(j=1;j<=n;j++)
	   {
	     p=p*x;

	   }
	 printf("\t%f to the power %f is equal to %f\n",x,n,p);
	}
	 printf("press Y to continue\n");
	 scanf(" %c",&c);
	}
	while(c=='y');
     getch();
}