#include <stdio.h>
#include <conio.h>
void main()
{
    clrscr();
    float n,i,p,j,x;
    char c,y;
    do
    {
     printf("\n\nenter any no\n");
     scanf(" %f",&n);
     for(i=1;i<=n;i++)
	{
	 p=1;
	 x=2*i;
	 for(j=1;j<=x;j++)
	   {
	     p=p*i;

	   }
	 printf("\t%f to the power %f is equal to %f\n",i,x,p);
	}
	 printf("press Y to continue\n");
	 scanf(" %c",&c);
	}
	while(c=='y');
     getch();
}